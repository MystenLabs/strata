[package]
name = "strata"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[features]
default = []
internal-profiling = ["store/internal-profiling"]

[dependencies]
core.workspace = true
store.workspace = true

[workspace]
members = [
    ".",
    "crates/core",
    "crates/gc",
    "crates/index",
    "crates/lsm",
    "crates/segment",
    "crates/store",
]
# Each of these is its own workspace. They depend on `typed-store`, which lives inside the
# MystenLabs/walrus repository, so keeping them out of this workspace keeps that git dependency out
# of this lockfile: building or testing Strata never fetches walrus.
exclude = [
    "crates/bench",
    "crates/port-compat",
    "crates/realistic-bench",
]
resolver = "3"

[workspace.package]
edition = "2024"
license = "Apache-2.0"
repository = "https://github.com/walrus-foundation/strata"

[workspace.dependencies]
bcs = "0.1.6"
bincode = "1.3.3"
fdlimit = "0.3.0"
libc = "0.2"
parking_lot = "0.12"
prometheus = "0.14.0"
rocksdb = { version = "0.22.0", default-features = false, features = ["lz4", "multi-threaded-cf", "snappy", "zlib", "zstd"] }
serde = { version = "1", features = ["derive"] }
serde_with = "3.20"
sha2 = "0.10"
core = { path = "crates/core" }
gc = { path = "crates/gc" }
index = { path = "crates/index" }
lsm = { path = "crates/lsm" }
segment = { path = "crates/segment" }
store = { path = "crates/store" }
tempfile = "3"
thiserror = "2"
tokio = { version = "1.49", features = ["macros", "rt-multi-thread", "sync", "time"] }
xxhash-rust = { version = "0.8.15", features = ["xxh3"] }
