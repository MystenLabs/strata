[package]
name = "index"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[features]
default = []
test-utils = ["core/test-utils"]

[dependencies]
bcs.workspace = true
bincode.workspace = true
fdlimit.workspace = true
rocksdb.workspace = true
serde.workspace = true
core.workspace = true
gc.workspace = true
lsm.workspace = true
thiserror.workspace = true

[dev-dependencies]
# Only used by the storage-port compatibility tests, which check that the port reads and
# writes exactly what the previous wrapper did. Not a dependency of the library itself.
typed-store.workspace = true
core = { workspace = true, features = ["test-utils"] }
tempfile.workspace = true
tokio.workspace = true
